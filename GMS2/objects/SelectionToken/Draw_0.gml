/// @description Insert description here
// You can write your code in this editor
if (blink && selection=-1) return;
draw_self();
if (boredom<=0){
//draw_set_color(image_blend);
draw_set_halign(fa_center);
	draw_text(x,y-16,text);
	draw_set_halign(fa_left);
}